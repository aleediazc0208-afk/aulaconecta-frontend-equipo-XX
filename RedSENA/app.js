/*==========================================================
    REDSENA — APP.JS
==========================================================*/

document.addEventListener("DOMContentLoaded", () => {

    inicializarStorage();
    inicializarUsuario();
    cargarUsuario();
    mostrarPublicaciones();
    iniciarEventos();

});
